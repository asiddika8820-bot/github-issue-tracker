    - 1️⃣ What is the difference between var, let, and const?
       *var is used in old version where let and const come from new version ES6.
        var  work inside the  same function, let ,const work only in the same block .
        const doesn't allow redeclaration,or value change.Var allow redeclaration.

    - 2️⃣ What is the spread operator (...)?
    spread operator is used for merge and copy of array and object element.
    - 3️⃣ What is the difference between map(), filter(), and forEach()?
     forEach() is a looping function,does not return any value.
     map() is used for transforming the elements of a existing array to a new array by performing some operation.

     filter() is used for selecting element from a array or object based on some condition.

    - 4️⃣ What is an arrow function?
    Arrow => is a modified and shorter function of version ES6.it works normal function.
    - 5️⃣ What are template literals?
  Its a new way to write strings and variable.as example backticks, ${}





